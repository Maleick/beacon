---
description: "Compatibility-only alias for /autoship-status"
allowed-tools: ["Bash", "Read", "Skill", "ToolSearch"]
compatibility: true
---

<autoship-status-cmd>

This compatibility-only alias is retained for older installs. Use the canonical public command `/autoship-status`.

Invoke the `autoship-status` skill via the Skill tool to display current AutoShip orchestration status.

</autoship-status-cmd>
