---
name: setup
description: Compatibility-only alias for /autoship-setup
compatibility: true
---

# /autoship-setup — compatibility-only alias

This compatibility-only alias is retained for older installs. Use the canonical public command `/autoship-setup`.

Run `commands/autoship-setup.md`.
